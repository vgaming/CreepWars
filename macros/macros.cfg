#textdomain wesnoth-Creep_War
{~add-ons/Creep_War_Dev/data/rules.cfg}
{~add-ons/Creep_War_Dev/macros/utils.cfg}
{~add-ons/Creep_War_Dev/shop/shop.cfg}
{~add-ons/Creep_War_Dev/macros/unit_template.cfg}
{~add-ons/Creep_War_Dev/macros/animation.cfg}
{~add-ons/Creep_War_Dev/macros/advancement.cfg}
{~add-ons/Creep_War_Dev/macros/hero.cfg}
{~add-ons/Creep_War_Dev/macros/creeps.cfg}
{~add-ons/Creep_War_Dev/macros/guard.cfg}
{~add-ons/Creep_War_Dev/macros/fog.cfg}

#define CWD_EVENT_SCENARIO_PRESTART
	[event]
		name=prestart
		{CWD_TEXT_INIT}
		{CWD_ITEMS_INIT}
		{CWD_SCORE_INIT}
		{CWD_SET_OBJECTIVES}
		{CWD_MIRROR_REPLACE}						#This must happen before era replaces traits at start
		{CWD_VARIABLES_INIT}
		[/event]
#enddef

#define CWD_EVENT_SCENARIO_START
	[event]
		name=start
		{CWD_KILL_SIDE {CWD_CREEP_SIDES}}
		{CWD_INIT_ADVANCEMENT}
		{CWD_MENU_ADVANCEMENT}
		{CWD_MENU_SHOP}
		{CWD_MENU_HELP}
		[/event]
#enddef

#define CWD_COLOUR_SCOREBOARD
255,165,0#enddef
#define CWD_HUMAN_PLAYER_SETTINGS TEAM SIDE PLAYERNUM
	[side]
		id=CW{SIDE}
		persistent=yes
		saveid=CW{SIDE}
		description={TEAM} Player {PLAYERNUM}
		fog=yes
		shroud=no
		team_name={TEAM}
		canrecruit=yes
		controller=human
		gold=100
		income=-2
		team_lock=yes
		gold_lock=yes
		income_lock=yes
		share_view=yes
	[/side]
#enddef

#define CWD_CREEP_PLAYER_SETTINGS TEAM SIDE
	[side]
		description={TEAM} Creeps
		no_leader=yes
		side={SIDE}
		team_name={TEAM}
		controller=ai
		gold=0
		income=-2
		allow_player=no
		team_lock=yes
		gold_lock=yes
		income_lock=yes
		share_view=yes
		canrecruit=yes
		[ai]
			aggression=0.90
			caution=0.10
			village_value=0.0
			leader_value=5.0
			[/ai]
		[/side]
#enddef

#define CWD_MAP_ADD_SHOP X_POS Y_POS
	[item]
		x={X_POS}
		y={Y_POS}
		image=terrain/castle/encampment/tent.png
		[/item]
	{SET_LABEL {X_POS} {Y_POS} $cw_text.shop}
#enddef

#define CWD_MENU_HELP
[set_menu_item]
	id=cw_menu_30_help
	description={CWD_TEXT_HELP}
	[command]
		[message]
			speaker=narrator
			side_for=$side_number
			message=$cw_text.start_message
			[option]
				message={CWD_TEXT_MENU_RETURN}
			[/option]
			[option]
				message=$cw_text.faq
				[command]
					[message]
						speaker=narrator
						side_for=$side_number
						message=$cw_text.faq_text
						[/message]
					[/command]
				[/option]
			[option]
				message=$cw_text.changelog
				[command]
					[message]
						speaker=narrator
						side_for=$side_number
						message="{~add-ons/Creep_War_Dev/changelog}"
						[/message]
					[/command]
				[/option]
			[/message]
		[/command]
	[/set_menu_item]
#enddef

#define CWD_MIRROR_REPLACE
	{CWD_STORE_HERO_T mirror_units "Fog Clearer" {CWD_PLAYER_SIDES} no}
	[while]
		{VARIABLE_CONDITIONAL mirror_units.length greater_than 0}
		[do]
			{VARIABLE i_start_side $mirror_units[0].side}
			{CWD_GET_OPPOSING_SIDE i_target_side i_start_side}
			[while]
				{VARIABLE_CONDITIONAL i_target_side  numerical_not_equals $i_start_side}
				{VARIABLE_CONDITIONAL b_found_target boolean_equals no}
				[do]
					{CWD_STORE_HERO temp_hero $i_target_side no}
					[if]
						{VARIABLE_CONDITIONAL temp_hero.type not_equals "Fog Clearer"}
						[then]
							{VARIABLE b_found_target yes}
							{VARIABLE temp_hero.name $mirror_units[0].name}
							{VARIABLE temp_hero.side $mirror_units[0].side}
							{VARIABLE temp_hero.id   $mirror_units[0].id}
							{VARIABLE temp_hero.x    $mirror_units[0].x}
							{VARIABLE temp_hero.y    $mirror_units[0].y}
							[kill]
								id=$mirror_units[0].id
								[/kill]
							[unstore_unit]
								variable=temp_hero
								[/unstore_unit]
							[/then]
						[else]
							{CWD_GET_OPPOSING_SIDE i_target_side i_target_side}
							[/else]
						[/if]
					{CLEAR_VARIABLE temp_hero}
					[/do]
				[/while]
			[if]
				{VARIABLE_CONDITIONAL b_found_target boolean_equals no}
				[then]
					{CWD_FIRE era_get_random_unit_type (
					unit_type_var=temp_unit_type)}
					{CWD_UNIT temp_hero $temp_unit_type $mirror_units[0].id $mirror_units[0].name $mirror_units[0].side $mirror_units[0].canrecruit $mirror_units[0].x $mirror_units[0].y}
					[kill]
						side=$mirror_units[0].side
						type=Fog Clearer
						[/kill]
					[unstore_unit]
						variable=temp_hero
						[/unstore_unit]
					{CLEAR_VARIABLE temp_unit_type,temp_hero}
					[/then]
				[/if]
			{CLEAR_VARIABLE i_start_side,i_target_side,b_found_target}
			{CWD_STORE_HERO_T mirror_units "Fog Clearer" {CWD_PLAYER_SIDES} no}
			[/do]
		[/while]
	{CLEAR_VARIABLE mirror_units}
#enddef

#define CWD_GET_PLAYER_SIDES_BY_TEAM SIDES TEAM_NAME
[store_side]
	team_name={TEAM_NAME}
	side={CWD_PLAYER_SIDES}
	variable={SIDES}
	[/store_side]
#enddef

#define CWD_GET_CREEP_SIDE_BY_TEAM SIDE TEAM_NAME
[store_side]
	team_name={TEAM_NAME}
	side={CWD_CREEP_SIDES}
	variable={SIDE}
	[/store_side]
#enddef

#define CWD_ITEMS_INIT
	{CWD_DEF_RESIST_ARRAY cw_resist}				# This array is used by some items
	{CWD_ITEMS_UPGRADE_INIT}
	{CWD_ITEMS_WEAPON_INIT}
	{CWD_ITEMS_ARMOR_INIT}
	{CWD_ITEMS_RESISTANCE_INIT}
#ifdef CWD_USE_REVIVE_GUARD
	{CWD_ITEMS_GUARD_REVIVE_INIT}
#endif
	{CWD_ITEMS_GUARD_INIT}
#ifdef CWD_USE_TEAMLEADER
	{CWD_ITEMS_TEAMLEADER_INIT}
#endif
#enddef

#define CWD_SCORE_ADD_FROM_SIDE SIDE SCORE
	{CWD_GET_TEAM_FROM_SIDE scoring_team {SIDE}}
	{VARIABLE_OP cw_score.kills.$scoring_team add {SCORE}}
	{CWD_UNIT_GUARD_INCREASE_HP $scoring_team 1 {SCORE}}
	{CWD_UNIT_GUARD_INCREASE_HP $scoring_team 2 {SCORE}}
	{CLEAR_VARIABLE scoring_team}
#enddef

#define CWD_GOLD_ADD_FROM_SIDE SIDE GOLD
	{CWD_GET_TEAM_FROM_SIDE earning_team {SIDE}}
	{CWD_GOLD_ADD_FROM_TEAM $earning_team {GOLD}}
	{CLEAR_VARIABLE earning_team}
#enddef

#define CWD_GOLD_ADD_FROM_TEAM TEAM GOLD
	{VARIABLE_OP cw_score.gold.{TEAM} add {GOLD}}
	{CWD_GET_PLAYER_SIDES_BY_TEAM earning_sides {TEAM}}
	{FOREACH earning_sides i}
		[gold]
			side=$earning_sides[$i].side
			amount={GOLD}
			[/gold]
		{NEXT i}
	{CLEAR_VARIABLE earning_sides}
#enddef

#define CWD_EVENT_START_MESSAGE
	[event]
		name=turn 1
		[message]
			caption=$cw_text.start_caption
			image="portraits/humans/transparent/general.png"
			message=$cw_text.start_message
			[/message]
		[/event]
#enddef

#define CWD_EVENT_PRINT_SCORE
[event]
	name=cw_event_print_score
	first_time_only=no
	{VARIABLE cw_score_colour "0,0,0"}	#	Fallback
	{CWD_GET_COLOUR_FROM_SIDE cw_score_colour $weapon.side}
	{CWD_ARRAY_COMMA cw_colour $cw_score_colour}
	[print]
#ifdef CWD_USE_THREE_TEAMS
		text="$cw_score.kills.West| : $cw_score.kills.South| : $cw_score.kills.East|"
#endif
#ifdef CWD_USE_TWO_TEAMS
		text="$cw_score.kills.West| : $cw_score.kills.East|"
#endif
		size=24
		red,green,blue=$cw_colour[0].value,$cw_colour[1].value,$cw_colour[2].value
		duration=110
		[/print]
	{CLEAR_VARIABLE cw_colour,cw_score_colour}
	{CWD_MAP_DISPLAY_SCORE}
	[/event]
#enddef

#define CWD_EVENT_ADD_KILLS_GOLD
[event]
	name=cw_event_add_kills_gold
	first_time_only=no
	{CWD_SCORE_ADD_FROM_SIDE $weapon.side2 $weapon.kills}
	{CWD_GOLD_ADD_FROM_SIDE  $weapon.side2 $weapon.gold}
#ifdef CWD_USE_THREE_TEAMS
	[if]
		{VARIABLE_CONDITIONAL cw_teams_alive greater_than_equal_to 3}
		[then]
			{CWD_GET_THIRD_TEAM_FROM_SIDES third_team $weapon.side1 $weapon.side2}
			{VARIABLE_OP weapon.kills divide 2}
			{VARIABLE_OP weapon.gold divide 2}
			{CWD_ADD_THIRD_TEAM_SCORE $third_team $weapon.kills}
			{CWD_GOLD_ADD_FROM_TEAM $third_team $weapon.gold}
			{CLEAR_VARIABLE third_team}
			[/then]
		[/if]
#endif
	{CWD_FIRE cw_event_print_score (side=$weapon.side2)}
	[/event]
#enddef

#define CWD_SCORE_INIT
	{VARIABLE cw_score.kills.West 0}
	{VARIABLE cw_score.kills.East 0}
	{VARIABLE cw_score.gold.West 0}
	{VARIABLE cw_score.gold.East 0}
#ifdef CWD_USE_THREE_TEAMS
	{VARIABLE cw_score.kills.South 0}
	{VARIABLE cw_score.gold.South 0}
#endif
#enddef

#define CWD_UNIT_INCREASE_HP ID AMOUNT
	[object]
		silent=yes
		[filter]
			id={ID}
			[/filter]
		{CWD_EFFECT_HITPOINTS {AMOUNT}}
	[/object]
#enddef

#define CWD_AI_AVOID SIDE AVOID_X AVOID_Y
{CLEAR_VARIABLE temp_sides}
{CWD_ARRAY_COMMA temp_sides {SIDE}}
{FOREACH temp_sides i}
	[modify_ai]
	    side=$temp_sides[$i].value
	    action=add
            path=aspect[avoid].facet
            [facet]
                engine=cpp
                name=standard_aspect
                [value]
                    x={AVOID_X}
                    y={AVOID_Y}
                    [/value]
                [/facet]
            [/modify_ai]
	{NEXT i}
    {CLEAR_VARIABLE temp_sides}
#enddef

#define CWD_VARIABLES_TEAM_INIT TEAM
	{VARIABLE cw_{TEAM}_creeps_max {CWD_MAX_CREEPS_PER_SIDE}}
    {VARIABLE cw_guard.{TEAM}.bonus.hitpoints 0}
    {VARIABLE cw_guard.{TEAM}.bonus.ranged.attacks 0}
    {VARIABLE cw_guard.{TEAM}.bonus.ranged.damage 0}
    {VARIABLE cw_guard.{TEAM}.bonus.melee.damage 0}
    {VARIABLE cw_guard.{TEAM}.bonus.melee.attacks 0}
#enddef

#define CWD_VARIABLES_INIT
    {CWD_VARIABLES_TEAM_INIT West}
    {CWD_VARIABLES_TEAM_INIT East}
#ifdef CWD_USE_THREE_TEAMS
    {CWD_VARIABLES_TEAM_INIT South}
	{VARIABLE cw_teams_alive 3}
#endif
    {CWD_ARRAY_COMMA temp_players {CWD_PLAYER_SIDES}}
    {FOREACH temp_players i}
        {VARIABLE cw_inventory.$temp_players[$i].value|.upgrades.melee.damage 0}
        {VARIABLE cw_inventory.$temp_players[$i].value|.upgrades.ranged.damage 0}
        {VARIABLE cw_inventory.$temp_players[$i].value|.upgrades.melee.attacks 0}
        {VARIABLE cw_inventory.$temp_players[$i].value|.upgrades.ranged.attacks 0}
        {FOREACH cw_items.types j}
            {VARIABLE cw_inventory.$temp_players[$i].value|.type.$cw_items.types[$j].name| 0}
            {FOREACH cw_items.$cw_items.types[$j].name| k}
                {VARIABLE cw_inventory.$temp_players[$i].value|.item.$cw_items.$cw_items.types[$j].name|[$k].uid 0}
                {NEXT k}
            {NEXT j}
        {NEXT i}
    {CLEAR_VARIABLE temp_players}
#enddef
